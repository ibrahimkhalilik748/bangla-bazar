import React from 'react';

const CheckBox = (props) => {
    const {name, price} = props.products;
    return (
        <div>
            <h1>lol</h1>
        </div>
    );
};

export default CheckBox;